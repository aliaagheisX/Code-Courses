const { DBConnection } = require('../config/database');

module.exports = {
    getAllInstructors: () => {
        return new Promise((resolve, reject) => {
            let queryString = `SELECT `
        })
    }
}